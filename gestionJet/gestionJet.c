#include "gestionJet.h"

void ajouter_jet(Jet *listeJet)
{
    /*Jet jet;
    int numeroJet = 0;

    // Aller au début du fichier pour lire le nombre actuel de clients
    rewind(listeJet);

    // Lire le nombre actuel de clients dans le fichier
    while (fscanf(listeJet, "%*d %*s %*s\n") != EOF)
    {
        numeroJet++;
    }

    // Incrémenter l'index du nouveau client
    jet.jet_Index = numeroJet + 1;

    printf("Saisissez le modèle du client : \n");
    scanf("%s", jet.jet_Modele);
    printf("Saisissez la gamme du jet : \n");
    scanf("%s", jet.jet_Gamme);

    // Aller à la fin du fichier pour ajouter le nouveau client
    fseek(listeJet, 0, SEEK_END);
    fprintf(listeJet, "%d %s %s\n", jet.jet_Index, jet.jet_Modele, jet.jet_Gamme);
    */
}
void modifier_jet(Jet *jet)
{
}
void supprimer_jet(Jet *jet)
{
}

void afficher_liste_jet_ordre_alphabetique(Jet *listeJet)
{
}